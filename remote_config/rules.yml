groups:
  - name: jetson_power_conversion
    interval: 1s
    rules:
      - record: consumed_power_watts
        expr: |
          consumed_power_raw_milliwatts{job="jetson-node"} * 0.001
        labels:
          unit: "watts"